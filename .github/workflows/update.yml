# OTA update workflow is disabled until expo-updates is compatible
# with react-native 0.81.5. Use apk/v* tags for all releases for now.
# This file is kept as a placeholder for future use.

name: EAS OTA Update (disabled)

on:
  workflow_dispatch: # manual trigger only, not automatic

jobs:
  update:
    name: Publish OTA JS Bundle
    runs-on: ubuntu-latest
    steps:
      - run: echo "OTA updates not yet configured"
